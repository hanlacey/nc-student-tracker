import React from "react";
import { Link } from "@reach/router";
import Button from "";
const Home = () => {
  return (
    <div>
      <Link to="/api/students">Student list</Link>
    </div>
  );
};

export default Home;
